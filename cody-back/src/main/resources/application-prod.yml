# Production Environment Configuration
# 프로덕션 환경 설정
# 민감한 정보는 시스템 환경변수로 설정하세요

spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?user=postgres.bdjdshpglpfpqoykpnhw&password=Codywardrobe0727!
    username: postgres.bdjdshpglpfpqoykpnhw
    password: Codywardrobe0727!
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        jdbc:
          lob:
            non_contextual_creation: true

server:
  port: ${SERVER_PORT:8080}

# CORS 설정 - 프로덕션 환경
cors:
  allowed-origins: https://cody-60p.pages.dev,http://localhost:3000,http://localhost:3001

logging:
  level:
    root: INFO
    com.cody.wardrobe: INFO
    org.hibernate.SQL: INFO

